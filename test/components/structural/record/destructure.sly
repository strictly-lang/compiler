data = / -> {
	init = { x = 1, deep = { y = 0 } }
	reducer = /state, x -> { x = state.x + x | state}
}

main = / ->
	\[{x, deep = { y = yValue }}, mainDispatch] <- model(data());
	$button{
		onclick = /_ -> mainDispatch(2)
	}
	$div
		"${x} ${yValue}"
