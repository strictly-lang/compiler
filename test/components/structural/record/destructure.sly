model:main{
	init = -> { x = 1, y = 0}
	reducer = state, x -> { x = state.x + x | state}
}

view
	#model ({x, y}, mainDispatch) <- main()
		button{
			onclick = _ -> mainDispatch(2)
		}
		div
			"${x} ${y}"
