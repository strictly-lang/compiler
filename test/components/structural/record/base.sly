type State = { x: Ingeger, y: Number };

reducer : \State, Number -> State
reducer = \state, x -> { x = state.x + x | state}

main : \{}, {} -> Output
main = \_, _ ->
	let action: Action;
	let state = action:scan(reducer, { x = 1, y = 0});

	$button{
		onclick = \_ -> action = 2;
	}
	$div
		"${mainState.x} ${mainState.y}"
