import /base/vendor/strictly/prelude/src/index.js (zipWithIndex)

model:main{
	init = -> [0, 1, 2, 3]
	reducer = state, multiply -> [ value * multiply | value <- state]
}

view
	#model ([first, second | rest], mainDispatch) <- main()
		button{
			onclick = _ -> mainDispatch(2)
		}
		span
			"first: ${first}, second: ${second}"
		#each (value, index) <- zipWithIndex(rest)
			div
				"${index}: ${value}"
		#else
			"empty"
