import \vendor\strictly\prelude\src\index.js (zipWithIndex)

main = \props ->
	$header
	each let [a, b] <- [[a, b] | let a <- [0, 1, 2], let b <- [3, 4]]
		$div
			"${a}: ${b}"
	else
		"empty"
	$footer
