model:main{
	init = -> { x = 0, y = 0}
	reducer = state, x -> { x = x | state}
}

view
	#model (mainState, mainDispatch) <- main()
		button{
			onclick = _ -> mainDispatch(2)
		}
		div
			"${mainState.x} ${mainState.y}"
