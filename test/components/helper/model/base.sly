data = / -> {
	init = ""
	reducer = /_, action -> action
}

main = /props ->
	\<mainState, mainDispatch> <- model(data());
	input{
		value = Text(mainState)
		oninput = /Text(value) -> mainDispatch(value)
	}
