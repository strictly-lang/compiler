store = {
	init = ""
	reducer : \String, String -> String
	reducer = \_, action -> action
}

main : \ {}, {} -> View
main = \_, _ ->
	let [mainState, mainDispatch] <- model(store);
	$input{
		type = "text"
		value = mainState
		oninput = \String[value] -> mainDispatch(value)
	}
