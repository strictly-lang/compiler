model:counter{
	init = _ -> 0
	reducer = Increment state -> state + 1
	reducer = Decrement state -> state - 1
}

view
	#model counter <- counter()
		button{
			onclick = _ -> counter(Decrement)
		}
			"Decrement"
		button{
			onclick = _ -> counter(Increment)
		}
			"Increment"
		"${main}"