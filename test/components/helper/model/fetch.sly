model data (id) -> fetch("/api/${id}", GET)
	action _ = action

view
	#if props.load
		#model data <- data(props.id)
			#match data
				#case Pending
					"Loading..."
				#case Result content
					"${content}"
				#case Error _
					"could not load"
	#else
		"not to be loaded"