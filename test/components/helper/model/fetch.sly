main = \props, _ ->
	if props.load then
		let stateState = fetch<String>!("/api/${id}");

		match stateState
			case \Pending ->
				"Loading..."
			case \Done(Ok( content))->
				"${content}"
			case \Done(Error( _ ))->
				"could not load"
	else
		"not to be loaded"
