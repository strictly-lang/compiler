model:data(
	init = id -> fetch("/api/${id}", GET)
	areducer = action _ _> action
)

view
	#if props.load
		#model data <- data(props.id)
			#match data
				#case Pending
					"Loading..."
				#case Result content
					"${content}"
				#case Error _
					"could not load"
	#else
		"not to be loaded"