data State = First, Second;

main : \{ foo: Boolean, }, {} -> Output
main = \props, _ ->
	$header
	let state = action:scan(reducer, First;
		if props.foo then
			match stateState
				case \First ->
					$div{
						onclick = \_ -> state = Second;
					}
		else
			$section
	$footer
