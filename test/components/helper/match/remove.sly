data = / -> {
	init = First
	reducer = /_, action -> action
}

main = /props ->
	$header
	\<dataState, dataDispatch> <- model(data());
		if props.foo
			match dataState
				case /First ->
					$div{
						onclick = /_ -> dataDispatch(Second)
					}
		else
			$section
	$footer
