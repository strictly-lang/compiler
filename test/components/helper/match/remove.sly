state = / -> {
	init = First
	reducer = /_, action -> action
}

main = /props ->
	$header
	\[stateState, stateDispatch] <- model(state());
		if props.foo then
			match stateState
				case /First ->
					$div{
						onclick = /_ -> stateDispatch(Second)
					}
		else
			$section
	$footer
