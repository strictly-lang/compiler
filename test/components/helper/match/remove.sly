store = {
	init = First
	reducer = \_, action -> action
}

main = \props ->
	$header
	let [stateState, stateDispatch] <- model(store);
		if props.foo then
			match stateState
				case \First ->
					$div{
						onclick = \_ -> stateDispatch(Second)
					}
		else
			$section
	$footer
