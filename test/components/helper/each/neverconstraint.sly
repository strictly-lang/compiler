data State = Odd(Integer), Even(Integer), Never(Integer);

#[webcomponent]
main : \{}, {} -> Output
main = \_, _ ->
	$header
	$section
		let Never(value) <- [Even(0), Odd(1), Even(2)];
		$div
			"${value}"
	$footer
