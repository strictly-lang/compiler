import \vendor\strictly\prelude\src\index.js (zipWithIndex)

main = \props ->
	$header
	if props.baz then
		each let [value, index] <- zipWithIndex(props.foo)
			$div
				"${index}-${value}-${props.bar}"
		else
			$span
				"Empty list ${props.bar}"
	else
		$section
	$footer
