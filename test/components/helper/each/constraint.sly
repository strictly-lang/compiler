data State = Odd(Number), Even(Number);

#[webcomponent]
main : \{}, {} -> Output
main = \_, _ ->
	let values = [Even(0), Odd(1), Even(2)];

	$header{
		onclick = \_ -> values = [Odd(0), Even(1), Even(2)];
	}
	$section
		let Even(value) <- values;
		$div
			"${value}"
	$footer
